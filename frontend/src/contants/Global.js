import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	// Defaults
	root: {
		flex: 1,
		alignItems: 'center'
	},

	// Container
	container: {
		flex: 1,
		alignItems: 'center',
		justifyContent: 'center'
	},
});
